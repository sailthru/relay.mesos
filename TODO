- get demo running properly using docker?
- have a development docker container that loads python code in a volume
- add README
- demo:
  - make metric func that tracks num tasks in mesos hook into demo.sh
  - add the relay web ui  (maybe by pushing a public docker image)
